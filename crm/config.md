# CRM configuration

Active adapter: **csv-log**

<!--
Change the line above to switch adapters: csv-log | airtable | none | <your-custom-adapter>
The name must match a file in crm/ (without .md).
Adapter-specific settings (base IDs, table names, field mappings) live inside
the adapter's own file, in its "Configuration" section.
-->
